var canvas = document.getElementById("gamecanvas");
var context = canvas.getContext("2d");

var tile_height = 64;
var tile_width = 128;

var Debug = true; //All references to debug should definitely be stripped in production
